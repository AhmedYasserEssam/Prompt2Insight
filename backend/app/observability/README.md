# Observability

Record request IDs, language, dialect, catalog/schema revisions, requested and actual models, fallback reason, SQL hash, policy result, row count, and latency.

Do not record credentials, protected result values, or unrestricted prompts.
